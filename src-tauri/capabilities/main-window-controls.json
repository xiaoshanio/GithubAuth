{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window-controls",
  "description": "Smallest permission set the custom themed title bar needs: drag, minimize, maximize/restore, and close the main window. No filesystem, shell, path, webview-creation, or event permissions are granted; vault access stays behind the app's own commands.",
  "windows": ["main"],
  "permissions": [
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-is-maximized",
    "core:window:allow-is-fullscreen",
    "core:window:allow-set-fullscreen",
    "core:window:allow-close"
  ]
}
