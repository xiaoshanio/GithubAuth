# GitHub Vault 设计方向

## 方向一：加密索引库

**Theme Name**：加密索引库

**Very Brief Intro**：将 GitHub 账户视为一套私密的身份索引，以低饱和黑色、细紫色状态线和密度克制的卡片表达秩序、可控与安全感。视觉继承参考图的横向工作台和卡片陈列，但以更清晰的隐私状态与资料层级来强化可用性。

**Probability**：0.073

## 方向二：石墨凭据册

**Theme Name**：石墨凭据册

**Very Brief Intro**：借鉴高级文具与档案夹的材质观感，以石墨灰、暖白标签与细腻纸张纹理减少工具的科技压迫感。界面更加安静，适合将账户视作长期维护的私人资产。

**Probability**：0.041

## 方向三：终端登记台

**Theme Name**：终端登记台

**Very Brief Intro**：将传统终端的高信息密度与现代卡片化操作融合，采用黑绿与扫描线细节突出输入、核验和复制动作。它更直接、更工程化，但不会使用夸张霓虹或赛博朋克视觉。

**Probability**：0.086

---

# 已选方向：加密索引库

## Design Movement

**现代信息索引（Modern Information Indexing）**。以参考图为视觉锚点：深色横向工作台、多列账户卡片、顶部细色状态线、右上角上下文操作，并将其转化为更精确的本地加密管理体验。

## Core Principles

1. **私密优先而非功能堆叠**：所有敏感字段默认隐藏，解锁、复制与显示都有明确的瞬时反馈。
2. **索引清晰而非表格化**：账户采用紧凑卡片，群组、风险与最近活动以可扫描的视觉标记表达。
3. **低噪声的深色层次**：使用近黑画布、石墨卡片与微弱边界，依靠留白和字重建立层级。
4. **复制动作可被信任**：右键菜单、倒计时与剪贴板提示令安全操作的去向明确可见。

## Color Philosophy

黑色不是空洞背景，而是承载私密内容的“保险箱内衬”。石墨灰负责构建可辨识的层次；冷紫色 **#A855F7** 作为唯一可识别品牌色，专用于选中、卡片顶端状态线、2FA 倒计时与高价值操作，避免让界面在视觉上显得躁动。

## Layout Paradigm

采用**左侧索引轨 + 横向工作台**。桌面端固定一条窄侧边栏承载群组与应用状态，主区不是传统居中面板，而是从顶部工具栏延伸到卡片瀑布式索引。窄屏下侧边栏缩为底部行动区，卡片从多列压缩为单列，保留单手操作的优先级。

## Signature Elements

1. **身份轨迹线**：每张账户卡片顶部均有一条冷紫色细线，代表处于已加密的索引状态。
2. **锁芯徽标**：以圆角方形中的抽象锁芯与分层卡片构成无文字图形标记，出现在应用头部、空状态和锁定页。
3. **环形 2FA 计时器**：仅在卡片或菜单中需要时出现，使用细环与秒数说明当前验证码的剩余时间。

## Interaction Philosophy

把“查看、复制、编辑”视为需要被确认的短暂动作。卡片悬停只出现必要的快捷控件；右键打开操作菜单；密码和 2FA 默认不会在列表中曝光；复制后以短暂的状态条明确反馈并自动清空剪贴板的建议时长。键盘搜索和 Escape 关闭菜单应保持即时，没有装饰性延迟。

## Animation

卡片首次显示采用 40–70ms 的轻微错峰淡入与 6px 上移复位，单次不超过 220ms。抽屉与对话框以 0.96 到 1 的缩放结合不透明度变化进入，遵循 `cubic-bezier(0.23, 1, 0.32, 1)`。复制、解锁等高频操作不使用长动画；悬停仅变化边界亮度和细微阴影。为“减少动态效果”设置关闭全部非必要过渡。

## Typography System

使用 **Manrope** 作为界面字体，以其紧凑、清晰的字腔支撑小尺寸密集信息；使用 **IBM Plex Mono** 显示验证码、创建日期、账户年龄与加密状态。页面标题使用 Manrope 700，分组标题使用 650，正文使用 500，辅助元数据使用 12–13px Mono，严格限制斜体和全大写。

## Brand Essence

**GitHub Vault 是为需要分组维护多个 GitHub 身份的人设计的本地加密索引册，以不上传账户机密的方式，将登录资料、2FA 和账户画像收束到一个安静的工作台中。**

人格关键词：克制、精确、可靠。

## Brand Voice

标题、CTA 与微文案应短促、确定、具备操作后果说明，不使用泛泛的欢迎语或营销话术。

示例：

> “解锁本地索引”

> “仅以用户名查询公开头像；不会传输邮箱、密码或 2FA。”

## Wordmark & Logo

图形标记由一个镂空锁芯与两张错位索引卡组成：外层方形暗示保管箱，内部切口暗示信息被分层保护。文字标记使用 Manrope 700 的定制字距，“Vault”以冷紫色强调，但图标本身不含文字。

## Signature Brand Color

**索引紫 #A855F7**。

## Style Decisions

锁定与初始化状态也必须体现“左侧索引、工作台、账户卡片”的核心语言：以分层记录剪影、单色紫色身份轨迹线与等宽安全元数据建立识别度。标题和 CTA 均以操作及后果为中心，例如“初始化本地账户索引”“密码无法恢复”；锁芯标记必须呈现受保护的核心与两张分层索引卡，避免孤立的通用锁图标。
